#include <stdio.h>
#include "esp_log.h"

#define TAG "SPI"

void mcu_spi_init(void)
{
    ESP_LOGI(TAG, "mcu_spi_init()\n\r");    
}

void mcu_spi_open(void)
{
    ESP_LOGI(TAG, "mcu_spi_open()\n\r");    
}